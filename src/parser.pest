digit = { '0'..'9' }
number = @{ digit+ }
atom = { number | brackets }
plus = { "+" }
times = { "*" }
addition = { atom ~ (plus ~ atom)* }
multiplication = { addition ~ (times ~ addition)* }
brackets = { "(" ~ expression ~ ")" }
expression = { multiplication } 
WHITESPACE = _{ " " }
